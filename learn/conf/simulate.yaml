defaults:
  - robot: ???
  - models: nn

#model_path: ???
dimension: all
save: true

policy:
  pid:
    params:
#      min_values: [1, .01, .1]
#      max_values: [500, 25, 50]
      terminal_cost: 1
      living_cost: .1

hydra:
  run:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}

bo:
  iterations: 2
  sim: true
  rollouts: 5
  variable_length: false
  max_length: 2
